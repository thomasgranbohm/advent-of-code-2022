fn part1(input: Vec<String>) {
	// Remove me
}

fn part2(input: Vec<String>) {
	// Remove me
}

fn main() {
	let input: Vec<String> = include_str!("./input.txt")
		.lines()
		.map(|a| a.to_string())
		.collect();

	part1(input.clone());
	part2(input.clone());
}
